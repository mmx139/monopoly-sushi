# 工作日志 2026-03-31

## 完成事项
- AI玩家UI选择支持（角色选择界面添加AI/玩家切换）
- 修复回合流程递归问题（移除checkAndSkipPlayer避免死循环）
- 问答/诗词卡系统（shared/cards.ts + CardModal.vue + game.ts集成）
- 前端房间UI创建（RoomScreen.vue支持创建/加入房间）
- Bug状态纠正（B00002/B00003/B00012改为Pending on Verify）
- Bug状态定义文档（docs/bugs/README.md）
- 道具卡系统 useItemCard() 实现16种道具效果
- 惩罚卡系统 applyPunishment() 实现14种惩罚效果
- App.vue 道具栏UI和交互
- CardModal.vue 三种结果(full/half/wrong)处理
- getPropertyById修复为find查找
- B00002 格子编号修复（绘制时使用(i+71)%72偏移匹配位置编号）
- B00012 棋盘布局优化（boardSize改为800，padding改为25）
- B00005 中间区域大圆圈覆盖问题（货币圆圈半径从0.8改为0.4）
- B00015 棋盘显示不全（boardSize从900改为800适配布局）
- useWebSocket.ts WebSocket客户端管理器
- game.ts 联机状态支持（isMultiplayer、roomId、setMultiplayerCallbacks）
- B00018 诗词卡只显示标题不显示全文（CardModal.vue）
- B00013 AI不处理惩罚卡（App.vue AI watch添加punishment处理）
- B00016 骰子不显示数字（Dice.vue先设值再动画；App.vue先rollDice()再等动画）
- B00017 道具卡状态实现（Player类型添加noItemTurns/tollX2Turns/doubleDice/hasImmunity）
- B00017 过路费翻倍、无法使用道具回合、免死金牌、破产检测等状态逻辑
- poetry_event.md用户操作流程更新（只显示标题）
- quiz_event.md题库描述修正（answers是数组非"/"分隔字符串）

## 进行中
- P2联机 WebSocket 客户端完整集成
- B00017 deed（地契）地皮选择UI待实现

## 决策记录
- Bug修复后状态改为Pending on Verify，只有用户可以改为Fixed
- AI答题：诗词60%完整、20%半首、20%错误
- 道具卡效果按需求文档001中4.3实现
- 惩罚卡效果按需求文档001中4.4实现
- 格子编号显示应用(i+71)%72偏移解决错位问题
- 诗词判断：先检查断句匹配(half)+100，再检查全诗匹配(full)+halfReward
- checkPoetryAnswer重写支持诗句分割匹配

## 问题记录
- P2联机完整集成尚未完成（WebSocket管理器已创建，集成到游戏逻辑待实现）
- B00019卡牌抽取动画尚未实现
- B00014问答卡抽取动画尚未实现

## 下一步
1. 完成P2联机WebSocket客户端与游戏逻辑的完整集成
2. 实现卡牌抽取动画效果
3. 实现deed（地契）地皮选择UI
4. 等待用户测试验证
